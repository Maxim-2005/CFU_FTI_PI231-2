// Подключение библиотек
#include <iostream>
#include <cmath>

using namespace std;

int main(){
    // Объявление переменных
    double m = 0;
    double h = 0;

    // Получение значений с консоли
    cin >> m, cin >> h;

    // Расчет + вывод
    cout << m / (h*h);
}
